<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- External storage files directory for downloads -->
    <external-files-path
        name="downloads"
        path="Download/" />

    <!-- Alternative if you use cache directory -->
    <cache-path
        name="cache"
        path="/" />

    <!-- External storage cache directory -->
    <external-cache-path
        name="external_cache"
        path="/" />
</paths>
